{
  "name": "gary",
  "version": "0.0.1",
  "description": "Gary: AI agent at 707 Labs. Picks up Linear tickets, opens PRs, fixes CI.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "deploy": "ssh mini 'bash -lc ~/Developer/gary/scripts/deploy-mini.sh'",
    "logs": "ssh mini 'tail -f ~/Library/Logs/gary/stdout.log'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@linear/sdk": "^39.0.0",
    "@octokit/auth-app": "^7.1.4",
    "@octokit/rest": "^21.1.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
